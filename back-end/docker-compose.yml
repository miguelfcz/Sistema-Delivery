version: "3.9"

services:
  db:
    image: postgres:17
    container_name: postgres-cardapio
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: deliverydb123
      POSTGRES_DB: cardapiodb
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  app:
    build: .
    container_name: node-cardapio
    ports:
      - "3000:3000"
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://postgres:cardapiodb123@db:5432/cardapiodb?schema=public
    volumes:
      - .:/app
      - /app/node_modules

volumes:
  db_data:
